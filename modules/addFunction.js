export function addOnclickToButton(btnFun) {//wyglada na to ze funkcja mi jest niepotrzebna, ale jeszcze zobaczymy.
  btnFun;
}

export function addTextAndButtonToTask(element, text, button) {
  element.innerHTML = text + button;
}